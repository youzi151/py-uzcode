"""Load middleware from .uzcode/mids/ (Phase 3+)."""

from __future__ import annotations

from pathlib import Path


def load_middleware(work_dir: Path, order: list[str]) -> list:
    """Placeholder — dynamic loading implemented in Phase 3."""
    return []
